# frozen_string_literal: true
# gemfile is created using teh command "bunlle init" then specify all the gem files that a project requires and
# to install these gems use "bundle install". Now is there is a certificat error then remove 's' from https
source "http://rubygems.org"
source "http://gems.github.com"

# gem "rails"
gem "watir-webdriver"
gem 'cucumber'
gem 'rspec'
gem 'rake'
gem 'page-object'
gem 'require_all'
gem 'pry'
gem 'data_magic'
gem 'chronic'
gem 'rails_helper'
gem 'colorize'

